type Query {
  users(offset: Int = 0, limit: Int = 10): UsersResult
  user(username: String!): User
}

type UsersResult {
  count: Int
  users: [User]
}

type User {
  username: String,
  email: String
}
